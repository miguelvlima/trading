{
  "$schema": "https://railpack.com/schema.json",
  "startCommand": "python -m alembic upgrade head && python -m app.scripts.seed_demo_data && python -m app.scripts.bootstrap_admin_user && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
}
